# AiPi-Clock-Mini 完整 SDK 包（解压即用）

> ## 📌 GitHub 仓库（XEMOWO/AiPi-Clock-Mini）说明
>
> GitHub 上放的是**项目源码 + Windows 零安装烧录包**（`Windows烧录/`），**不含**这个 zip 包本体，也不含 SDK：
>
> - 只想烧录：直接进 `Windows烧录/` 双击 `一键烧录.bat` 即可，**不需要这个包**，什么都不用装；
> - 要编译/二次开发：按仓库 `README.md` 的**「用官方 SDK 自己搭环境」/「Set up a custom SDK environment」**章节，从 gitee 克隆官方 SDK 并配置 `BL60X_SDK_PATH`（README 中英两版都有）；
> - 需要本「完整 SDK 包」（解压即用、内置工具链与颜色补丁）时，认准名字带 *完整SDK包* 的 zip 文件本身。
>
> ---

这是 **Ai-Thinker-WB2 官方 SDK** + **AiPi-Clock-Mini 桌面时钟项目** 的完整打包。
**Linux 工具链、Windows(MSYS) 工具链、烧录工具、屏幕颜色补丁全部内置**，
不需要克隆代码、不需要装 git、不需要打补丁。

- 芯片: Ai-WB2 (BL602)
- 屏幕: ST7789 172x320 彩屏
- 功能: LVGL 图形界面 + WiFi + SNTP 时间

## 系统要求（Linux 和 Windows 都能编译烧录）

| 方式 | 能做什么 | 额外要装什么 |
|------|---------|-------------|
| **Linux** | ✅ 编译 + 烧录 | 什么都不用装 |
| **Windows + WSL2** | ✅ 编译 + 烧录 | 装 Ubuntu + VSCode 的 WSL 插件，首次转发串口 |
| **Windows 原生** | ✅ 编译 + 烧录 | 装 MSYS2（10 分钟）；只想烧录则什么都不用装 |

---

## 一、Windows 原生用户

### 方案 A：只想烧录（零安装，最省事）

解压后进 `Windows烧录/` 文件夹，**双击 `一键烧录.bat`**：

1. 按提示输入串口号（设备管理器 → 端口 COM 和 LPT 里查看，如 `COM3`）
2. 提示操作板子时：**按住 BOOT → 按一下 RST → 松开 BOOT**
3. 等它烧完就完成了

> 设备管理器里看不到串口 → 装 CH340 驱动，重新插拔 USB。

### 方案 B：完整编译 + 烧录（装一次 MSYS2，一劳永逸）

包内已内置 Windows 版编译工具链（`toolchain/riscv/MSYS/`），只差一个 MSYS2 终端环境：

```bash
# 1. 安装 MSYS2（二选一）
winget install MSYS2.MSYS2        # 或去 https://www.msys2.org 下载安装

# 2. 开始菜单打开 "MSYS2 MSYS" 终端，装编译工具（只需一次）
pacman -S --needed base-devel python

# 3. 进入项目（注意: C:\ 盘在 MSYS 里写 /c/）
cd /c/你的解压路径/WB2-clock-SDK/applications/xemowo/WB2-clock2

# 4. 一键烧录（自动编译 → 列出 COM 口 → 选波特率 → 烧录）
./flash.sh
```

> 首次运行 `pacman` 如果提示安装密钥，按提示执行 `pacman-key --init` 和 `pacman-key --populate` 即可。
> 编译时如果提示缺某个工具，`pacman -S 工具名` 补装。

## 二、三步上手（Linux / Windows + WSL2）

```bash
# 1. 解压（Windows 上右键解压即可）
unzip WB2-clock完整SDK包.zip

# 2. 进入项目
cd WB2-clock-SDK/applications/xemowo/WB2-clock2

# 3. 一键烧录（自动编译 → 选串口 → 选波特率 → 烧录）
./flash.sh
```

或者直接运行包根目录的：

```bash
./一键烧录.sh
```

> Windows + WSL2 用户第一次用，需要把串口转发进 WSL：
> ```
> winget install usbipd
> usbipd bind --auto-attach
> usbipd attach --wsl    # 插入板子后运行
> ```
> 然后重新插拔板子，`ls /dev/ttyUSB*` 能看到设备就成功了。

## 三、用 VSCode 开发（三个平台都支持）

项目自带 VSCode 一键任务，装好环境后**按 `Ctrl+Shift+B` 就能直接烧录**：

1. VSCode 打开 `WB2-clock-SDK/applications/xemowo/WB2-clock2` 文件夹
   - **WSL2 用户**：装 VSCode 插件 "WSL"(Remote-WSL)，然后从 WSL 里 `code .` 打开
2. 按 `Ctrl+Shift+B` → 选任务：
   - `一键烧录 (编译+选串口+下载)` —— **Linux / WSL 用户选这个**
   - `一键烧录 - Windows原生 (需装MSYS2)` —— **Windows 原生用户选这个**（前提是装好了 MSYS2）
3. 在弹出的终端里按提示选串口、波特率，完事

> 任务里的路径转换、串口识别都是自动的，无需配置。
> 想只编译不烧录：`Ctrl+Shift+B` 选 `仅编译 (make -j8)`。

## 烧录前接线

| 屏幕 ST7789 | WB2 引脚 |
|------------|---------|
| SCL (SCK)  | GPIO 3  |
| SDA (MOSI) | GPIO 12 |
| CS         | GPIO 14 |
| DC         | GPIO 4  |
| RST        | GPIO 17 |
| VCC        | 3.3V    |
| GND        | GND     |

## 烧录时卡住怎么办

烧录工具等待设备时，**按住板上的 BOOT 键 → 按一下 RST 键 → 松开 BOOT**，
就会进入下载模式，再点一次烧录即可。

## WiFi 怎么改

### 方式一：手机配网（推荐，不用连电脑）

**累计按 5 次 EN(RST) 键**（配网成功后会清零重新计数），屏幕进入配网模式：

1. 屏幕显示热点名（如 `Clock-Mini-A1B2`，MAC 后 4 位），**热点无密码**，手机直接连接
2. 手机连接该热点，会自动弹出配网页（没自动弹出就手动打开浏览器访问
   `http://192.168.169.1`；若之前连过该热点没弹，可在手机 WiFi 设置里
   选"忽略此网络"后重新连接）
3. 页面里选/填路由器 WiFi 和密码，点 Connect
4. 屏幕提示"配网成功"后自动重启，正常显示时间（计数已清零）

> - 累计开机 5 次进入配网；**配网成功、或正常运行 3 分钟后计数自动清零**——
>   正常使用（插拔、断电重启）不会误进配网，只有 3 分钟内连按 5 次复位
>   才会触发
> - 进入配网后按 EN 或断电重插会**继续**配网（直到配网成功）
> - 配网模式 10 分钟无操作自动退出，回到正常模式

### 方式二：改默认值（烧录前）

**出厂不预置任何 WiFi**（`DEFAULT_SSID` 为空），首次开机直接进入手机配网模式。
想改默认 SSID/密码：
编辑 `applications/xemowo/WB2-clock2/WB2-clock2/xcmd/cfg_store.c` 里的
`DEFAULT_SSID` / `DEFAULT_PWD`，然后重新烧录（配置存在 flash 里，永久保存）。

## 常见问题

| 现象 | 解决 |
|------|------|
| 烧录卡在等待设备 | 按住 BOOT 再按 RST，重新进入下载模式 |
| 图片颜色全错 | 不会发生 —— 补丁已内置（`LV_COLOR_16_SWAP 0`） |
| 传开机图 | 用配套上位机 `serial_tool`（WiFi 配置页 → 开机图卡片）：选图自动缩放为 320x220 全屏上传，上电显示 2 秒后正常显示时钟；可"清除开机图"恢复无开机图 |
| 找不到串口 | Windows 原生: 装 CH340 驱动; WSL2: 看上面 usbipd 步骤 |
| 报错 `No such file: ...\chips\bl602\eflash_loader\...` | `Windows烧录/` 文件夹必须完整（含 `chips/` 目录），别单独拷 exe 出来 |
| 报错 `BFLB FLASH MATCH TYPE FAIL` | 板子的 flash 型号不在内置配置里。报错日志 `readdata: b'xxxxxxxx'` 取前 6 位（如 `5e4016`），把 `Windows烧录/utils/flash/bl602/` 里任意一个 `.conf` 复制一份，改名为 `<型号>_<前6位>.conf`（如 `MYFLASH_5e4016.conf`）再烧 |
| Windows 原生编译报缺工具 | MSYS2 终端里 `pacman -S 工具名` 补装 |
| VSCode 按 Ctrl+Shift+B 没反应 | 确认打开的是 `applications/xemowo/WB2-clock2` 文件夹本身 |
| 想改界面/进阶开发 | 看 `applications/xemowo/WB2-clock2/README.md` |

---

## 包内容

```
WB2-clock-SDK/
├── 使用说明.md                  ← 就是本文件
├── 一键烧录.sh                  ← Linux/WSL/MSYS 自动定位项目并烧录
├── Windows烧录/                 ← Windows 原生: 双击一键烧录.bat 即可(自包含)
│   ├── 一键烧录.bat / WB2-clock2.bin / bflb_iot_tool.exe
│   ├── chips/ 分区表等 (烧录工具依赖, 已内置)
│   └── utils/ 各型号 flash 配置 (flash 匹配用, 已内置 70+ 型号)
├── README.md                    ← SDK 官方说明（可忽略）
├── make_scripts_riscv/          ← 构建系统
├── components/                  ← SDK 组件（含 LVGL，已打颜色补丁）
├── toolchain/riscv/Linux/       ← RISC-V 编译工具链（Linux/WSL 用）
├── toolchain/riscv/MSYS/        ← RISC-V 编译工具链（Windows 原生用）
├── tools/flash_tool/            ← 烧录工具（内置，跨平台）
└── applications/xemowo/WB2-clock2/
    ├── .vscode/                 ← VSCode 一键任务（Ctrl+Shift+B 烧录）
    ├── Makefile / proj_config.mk
    ├── flash.sh                 ← 一键: 编译+选串口+烧录（Win/Linux 通用）
    ├── WB2-clock2/              ← 源码（main.c、custom/、generated/、xcmd/）
    └── README.md                ← 开发者详细文档
```

> 注：为了减小体积，包内删掉了 SDK 的 git 历史、示例项目（applications 下其他目录）和编译产物。这些都不影响本项目的编译烧录。若需要完整 SDK，去 https://gitee.com/Ai-Thinker-Open/Ai-Thinker-WB2 克隆，并按 `applications/xemowo/WB2-clock2/README.md` 的"开发者步骤"操作。
